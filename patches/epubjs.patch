diff --git a/lib/contents.js b/lib/contents.js
index 8392d8710b768cab227120ccf32ea8347e213e83..eca21d3ec5eb6ee5184e9981b823bbf3bdff35da 100644
--- a/lib/contents.js
+++ b/lib/contents.js
@@ -164,6 +164,22 @@ class Contents {
       width += border.width;
     }
 
+    // Sanity check: Range.getBoundingClientRect() can return inflated widths
+    // in CSS column layouts when oversized images are present.
+    // Fall back to measuring max child element width if range seems wrong.
+    var scrollW = content.scrollWidth;
+    if (scrollW > 0 && width > scrollW * 3) {
+      var maxChildW = 0;
+      var children = content.children;
+      for (var i = 0; i < children.length; i++) {
+        var cw = children[i].getBoundingClientRect().width;
+        if (cw > maxChildW) maxChildW = cw;
+      }
+      if (maxChildW > 0 && maxChildW < width) {
+        width = maxChildW;
+      }
+    }
+
     return Math.round(width);
   }
   /**
@@ -1138,7 +1154,41 @@ class Contents {
     this.css("max-width", "inherit");
     this.css(COLUMN_FILL, "auto");
     this.css(COLUMN_GAP, gap + "px");
-    this.css(COLUMN_WIDTH, columnWidth + "px"); // Fix glyph clipping in WebKit
+    this.css(COLUMN_WIDTH, columnWidth + "px");
+
+    // Constrain images/SVGs to column width before expand() measures textWidth.
+    // Without this, wide images inflate the measurement causing phantom blank pages.
+    // We set BOTH inline styles (immediate effect) and stylesheet rules (persistent).
+    var horizontalPadding = gap; // gap/2 on each side
+    var maxW = (columnWidth - horizontalPadding) + "px";
+
+    // 1) Inline styles on existing elements for immediate layout effect
+    var images = this.document.querySelectorAll("img");
+    for (var i = 0; i < images.length; i++) {
+      images[i].style.maxWidth = maxW;
+      images[i].style.height = "auto";
+      images[i].style.boxSizing = "border-box";
+    }
+    var svgs = this.document.querySelectorAll("svg");
+    for (var j = 0; j < svgs.length; j++) {
+      svgs[j].style.maxWidth = maxW;
+      svgs[j].style.height = "auto";
+    }
+
+    // 2) Stylesheet rules for elements loaded later
+    this.addStylesheetRules({
+      "img" : {
+        "max-width": maxW + "!important",
+        "height": "auto",
+        "box-sizing": "border-box"
+      },
+      "svg" : {
+        "max-width": maxW + "!important",
+        "height": "auto"
+      }
+    });
+
+    // Fix glyph clipping in WebKit
     // https://github.com/futurepress/epub.js/issues/983
 
     this.css("-webkit-line-box-contain", "block glyphs replaced");
diff --git a/lib/layout.js b/lib/layout.js
index 3b925937c08f34af3233d65bf1ec367e2bb7f013..4eaa34d42d5ae5cad082f24f2642a09a23a31b7f 100644
--- a/lib/layout.js
+++ b/lib/layout.js
@@ -124,7 +124,9 @@ class Layout {
     var pageWidth;
     var delta;
 
-    if (this._spread && width >= this._minSpreadWidth) {
+    if (width >= 2560) {
+      divisor = 3;
+    } else if (this._spread && width >= this._minSpreadWidth) {
       divisor = 2;
     } else {
       divisor = 1;
@@ -134,6 +136,10 @@ class Layout {
       gap = section % 2 === 0 ? section : section - 1;
     }
 
+    if (divisor >= 3 && !(_gap >= 0)) {
+      gap = 60;
+    }
+
     if (this.name === "pre-paginated") {
       gap = 0;
     } //-- Double Page
diff --git a/src/contents.js b/src/contents.js
index 3effe7252459218ad3d83d1b47ef338007a6d66a..960c6e6fc290fb3a3aadc6efb51f8695afbfee6a 100644
--- a/src/contents.js
+++ b/src/contents.js
@@ -162,6 +162,22 @@ class Contents {
 			width += border.width;
 		}
 
+		// Sanity check: Range.getBoundingClientRect() can return inflated widths
+		// in CSS column layouts when oversized images are present.
+		// Fall back to measuring max child element width if range seems wrong.
+		let scrollW = content.scrollWidth;
+		if (scrollW > 0 && width > scrollW * 3) {
+			let maxChildW = 0;
+			let children = content.children;
+			for (let i = 0; i < children.length; i++) {
+				let cw = children[i].getBoundingClientRect().width;
+				if (cw > maxChildW) maxChildW = cw;
+			}
+			if (maxChildW > 0 && maxChildW < width) {
+				width = maxChildW;
+			}
+		}
+
 		return Math.round(width);
 	}
 
@@ -1097,6 +1113,38 @@ class Contents {
 		this.css(COLUMN_GAP, gap+"px");
 		this.css(COLUMN_WIDTH, columnWidth+"px");
 
+		// Constrain images/SVGs to column width before expand() measures textWidth.
+		// Without this, wide images inflate the measurement causing phantom blank pages.
+		// We set BOTH inline styles (immediate effect) and stylesheet rules (persistent).
+		let horizontalPadding = gap; // gap/2 on each side
+		let maxW = (columnWidth - horizontalPadding) + "px";
+
+		// 1) Inline styles on existing elements for immediate layout effect
+		let images = this.document.querySelectorAll("img");
+		for (let i = 0; i < images.length; i++) {
+			images[i].style.maxWidth = maxW;
+			images[i].style.height = "auto";
+			images[i].style.boxSizing = "border-box";
+		}
+		let svgs = this.document.querySelectorAll("svg");
+		for (let i = 0; i < svgs.length; i++) {
+			svgs[i].style.maxWidth = maxW;
+			svgs[i].style.height = "auto";
+		}
+
+		// 2) Stylesheet rules for elements loaded later
+		this.addStylesheetRules({
+			"img" : {
+				"max-width": maxW + "!important",
+				"height": "auto",
+				"box-sizing": "border-box"
+			},
+			"svg" : {
+				"max-width": maxW + "!important",
+				"height": "auto"
+			}
+		});
+
 		// Fix glyph clipping in WebKit
 		// https://github.com/futurepress/epub.js/issues/983
 		this.css("-webkit-line-box-contain", "block glyphs replaced");
diff --git a/src/layout.js b/src/layout.js
index 4f16a0f1d190f774ea6ae8e002c82fdf0e62821f..e999bfe60783f4ac9360af8e8a4386a3d70fc1db 100644
--- a/src/layout.js
+++ b/src/layout.js
@@ -117,7 +117,9 @@ class Layout {
 		var pageWidth;
 		var delta;
 
-		if (this._spread && width >= this._minSpreadWidth) {
+		if (width >= 2560) {
+			divisor = 3;
+		} else if (this._spread && width >= this._minSpreadWidth) {
 			divisor = 2;
 		} else {
 			divisor = 1;
@@ -127,6 +129,10 @@ class Layout {
 			gap = ((section % 2 === 0) ? section : section - 1);
 		}
 
+		if (divisor >= 3 && !(_gap >= 0)) {
+			gap = 60;
+		}
+
 		if (this.name === "pre-paginated" ) {
 			gap = 0;
 		}
